{
  "name": "twofold",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "compile": "pnpm run --reporter=append-only -r --aggregate-output compile",
    "test": "pnpm run --reporter=append-only -r --aggregate-output test",
    "lint": "pnpm run --reporter=append-only -r --aggregate-output lint"
  },
  "keywords": [],
  "author": "Ryan Toronto",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "typescript": "catalog:"
  }
}
